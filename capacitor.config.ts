import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.solomonarega.smartschool',
  appName: 'SmartSchool',
  webDir: 'www'
};

export default config;
